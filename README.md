# Lottery

1~45 중 6개 번호를 뽑는 단일 HTML 로또 추첨기입니다.

Supabase `lotto_draw` 테이블에 추첨 기록을 저장합니다.

## 실행

`index.html`을 브라우저에서 열면 됩니다.

## Supabase

- 연결 프로젝트: `localsync`
- 테이블: `public.lotto_draw` (추첨 기록)
- 마이그레이션: `supabase/migrations/`
